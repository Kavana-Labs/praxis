This is the code executor source.

## Installation
```bash
source venv/bin/activate  # activating virtualenv
pip install -r requirements.txt # installing dependencies
uvicorn app.main:app --reload
```